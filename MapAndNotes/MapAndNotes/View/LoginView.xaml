<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="MapAndNotes.View.LoginView">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Username"/>
            <Entry x:Name="usernameEntry" Text="{Binding Username}" Placeholder="username"/>
            <Label Text="Password"/>
            <Entry x:Name="passwordEntry" Text="{Binding Password}" IsPassword="True"/>
            <Button Text="Connexion" Command="{Binding SubmitCommand}"/>
            <Button Text="Inscription" Command="{Binding VersInscriptionCommand}"/>

            <Label x:Name="messageLabel"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>

